"""Placeholder — backend factory tests removed.

``omop_emb.backends.factory`` was not carried forward in the refactor.
Backend construction is done directly via ``SQLiteVecBackend(emb_engine=...)``
or ``PGVectorEmbeddingBackend(emb_engine=...)``.
"""
