"""omop_emb storage layer.

This package contains optional export/sidecar stores that sit on top of
a backend.  Currently: the FAISS index store (``storage/faiss/``).

Core backends live in ``omop_emb.backends``.
"""
