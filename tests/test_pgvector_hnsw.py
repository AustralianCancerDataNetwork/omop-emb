"""Integration tests for pgvector HNSW backend — moved into test_pgvector.py.

This file is kept as a redirect so existing test discovery doesn't break.
All HNSW integration tests now live in ``TestPGVectorHNSWBackend`` inside
``test_pgvector.py``.
"""
